Copie este conteúdo para um arquivo .env na raiz do projeto:

EXPO_PUBLIC_API_URL=https://webapp-motoconnect-555466.azurewebsites.net/api

Para desenvolvimento local, substitua pela URL do seu backend local:
EXPO_PUBLIC_API_URL=http://192.168.1.100:5000/api

Para descobrir seu IP local:
- Linux/Mac: ip addr | grep "inet " ou ifconfig
- Windows: ipconfig

